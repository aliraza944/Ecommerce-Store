import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  root: {
    width: "100%",
  },
  cardsection: {
    display: "flex",
    flexDirection: "row",

    width: "100%",
  },
}));
export default useStyles;
